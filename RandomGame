function randomGame(){
    let num;
    let times = 0;
    let interval = setInterval(function(){
      num = Math.random();
      times++
      if(num > .75) {
        clearInterval(interval);
        console.log("It took " + times + " try/tries.");
      }
    },1000)
  }
